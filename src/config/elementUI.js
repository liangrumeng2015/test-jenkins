import Vue from 'vue'
import {
  Button,
  Container,
  Header,
  Aside,
  Main,
  Card,
  Pagination
} from 'element-ui'

Vue.use(Button).use(Container).use(Header).use(Aside).use(Main).use(Card).use(Pagination)
